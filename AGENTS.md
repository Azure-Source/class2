# 高2024级2027届2班班级网站 - 需求拆解文档

## 产品概述

- **产品类型**: 班级官方展示网站
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 班级同学、老师、家长及校友
- **核心价值**: 记录班级成长历程，展示班级风采风貌，搭建班级线上精神家园
- **界面语言**: 中文
- **主题偏好**: 浅色（青春、活力、温暖风格，蓝青主色调 + 橙黄点缀）
- **导航模式**: 锚点导航
- **导航布局**: Topbar（顶部固定导航栏）

---

## 页面结构总览

> **说明**：单页应用（SPA），所有模块以锚点区块形式组织在同一页面内

**页面文件**: `HomePage.tsx`

| 区块名称 | 锚点 | 区块说明 |
|---------|------|---------|
| 首页 Hero | `#hero` | 首屏横幅，展示班级名称、口号、毕业倒计时、快速导航卡片、班级简介 |
| 班级介绍 | `#about` | 班级基本信息、班风班训、班级理念与特色 |
| 班级成员 | `#members` | 同学展示墙、班委团队、任课老师介绍 |
| 班级动态 | `#news` | 最新公告列表、班级活动回顾 |
| 班级相册 | `#gallery` | 分类图片网格、灯箱放大查看 |
| 课程表 | `#schedule` | 本周课程表，含早读/上午/下午/晚自习时段 |
| 荣誉墙 | `#honors` | 班级荣誉与奖项时间线展示 |
| 留言板 | `#guestbook` | 留言展示列表 + 留言输入表单 |
| 页脚 | `#footer` | 联系方式、版权信息、回到顶部按钮 |

---

## 页面布局建议

- **布局模式**: 单页滚动式 + 顶部固定导航栏，采用 Flexbox/Grid 响应式布局
- **视觉重心**: 首屏 Hero 区域为视觉重心，使用大气横幅 + 主标题突出班级形象；后续区块以卡片网格形式均衡分布
- **结果承载区**: 留言板模块为交互结果承载区，初始态展示 8-10 条模拟留言；提交新留言后插入列表顶部并显示成功反馈
- **响应式策略**: 桌面端多列网格 → 平板端两列 → 移动端单列堆叠；导航栏在移动端转为汉堡菜单

---

## 导航配置

- **导航布局**: Topbar（顶部固定，滚动时背景透明度变化）
- **导航项**:

| 导航文字 | 锚点 |
|---------|------|
| 首页 | `#hero` |
| 班级介绍 | `#about` |
| 班级成员 | `#members` |
| 班级动态 | `#news` |
| 班级相册 | `#gallery` |
| 课程表 | `#schedule` |
| 荣誉墙 | `#honors` |
| 留言板 | `#guestbook` |

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 班级基本信息 | demo-mock | `src/data/classInfo.ts` 常量定义班级名称、入学/毕业年份、人数、班主任等 | ✅ 本身就是 mock |
| 同学展示数据 | demo-mock | `src/data/students.ts` 模拟 15-20 位同学数据（姓名、星座、兴趣爱好、格言） | ✅ 本身就是 mock |
| 班委与老师数据 | demo-mock | `src/data/teachers.ts` + 班委职务映射 | ✅ 本身就是 mock |
| 公告与活动数据 | demo-mock | `src/data/announcements.ts` 模拟 5-8 条公告 + 活动记录 | ✅ 本身就是 mock |
| 相册图片数据 | demo-mock | `src/data/gallery.ts` 模拟分类图片数组，使用 Unsplash/Pexels 等高质量网络图片 URL | ✅ 本身就是 mock |
| 课程表数据 | demo-mock | `src/data/schedule.ts` 按周一至周五 + 时段的二维结构定义 | ✅ 本身就是 mock |
| 荣誉奖项数据 | demo-mock | `src/data/honors.ts` 模拟 6-8 个荣誉，含时间、名称、描述 | ✅ 本身就是 mock |
| 留言板留言 | local-persist | localStorage key=`__class2_guestbook_messages`，初始化合并 mock 数据 + 本地存储数据 | 初始 8-10 条 source='mock' 示例留言 |
| 毕业倒计时计算 | demo-mock | 前端实时计算当前日期到 2027 年毕业日期的天数差 | 无需 mock，纯计算 |

> 所有展示类数据均为 demo-mock，符合用户"所有数据使用模拟数据"的明确要求；留言板为前端模拟提交 + localStorage 持久化，符合"前端模拟，提交后显示在列表顶部"的需求。

---

## 功能列表

### 首页 Hero 区块
- **页面目标**: 营造班级第一印象，提供快速导航入口
- **功能点**:
  - **毕业倒计时**: 实时计算并展示距离 2027 年毕业的天数，数字带动效
  - **快速导航卡片**: 4-6 张卡片链接到主要模块，点击平滑滚动到对应锚点
  - **班级简介**: 展示班级定位和精神风貌的文字段落
  - **首屏视差效果**: 背景图随滚动产生轻微视差，增强纵深感

### 班级介绍区块
- **页面目标**: 全面介绍班级基本情况和理念
- **功能点**:
  - **信息卡片展示**: 以卡片形式展示班级人数、班主任、任课老师数量等关键数据
  - **班风班训展示**: 突出展示班级口号和核心理念，配合图标增强视觉
  - **班级特色列表**: 以图标 + 文字形式列出班级特色（如学习氛围、文体活动等）

### 班级成员区块
- **页面目标**: 展示班级全体成员风采
- **功能点**:
  - **同学卡片网格**: 15-20 张同学卡片，悬停时有上浮 + 阴影微动效，展示姓名、星座、兴趣爱好、个人格言
  - **班委团队高亮**: 顶部突出展示班委成员（班长、副班长、学习委员等），带职务标签
  - **任课老师展示**: 教师卡片展示科目、姓名、教学风格描述
  - **Tab 切换**: 同学 / 班委 / 老师 三类成员通过 Tab 切换展示

### 班级动态区块
- **页面目标**: 发布班级公告和活动记录
- **功能点**:
  - **公告列表**: 时间线形式展示 5-8 条公告，含标题、日期、内容摘要，支持展开/收起详情
  - **活动回顾卡片**: 以卡片形式展示重要活动（运动会、班会、社会实践等），含活动图片、标题、时间、简述

### 班级相册区块
- **页面目标**: 分类展示班级活动照片
- **功能点**:
  - **分类标签筛选**: 集体活动 / 运动会 / 文艺汇演 / 日常学习 / 团建活动 五个分类标签，点击切换筛选
  - **图片网格布局**: CSS Grid 瀑布流/等宽网格展示图片，悬停有缩放+遮罩效果
  - **灯箱放大查看**: 点击图片弹出灯箱，支持左右切换浏览、关闭按钮、键盘 ESC 退出
  - **图片懒加载**: 滚动到视口才加载图片，提升首屏性能

### 课程表区块
- **页面目标**: 展示本周课程安排
- **功能点**:
  - **课程表表格**: 周一至周五 × 早读/上午4节/下午3节/晚自习 的表格布局
  - **科目颜色标签**: 不同科目用不同颜色背景标签区分，一目了然
  - **周次/日期显示**: 展示当前周次和日期范围

### 荣誉墙区块
- **页面目标**: 展示班级获得的各项荣誉
- **功能点**:
  - **时间线展示**: 垂直时间线形式展示 6-8 个荣誉奖项，按时间倒序排列
  - **荣誉卡片**: 每个荣誉含奖项名称、颁发时间、颁发单位、获奖描述，带图标和装饰元素
  - **滚动入场动画**: 滚动到视口时时间线节点依次出现

### 留言板区块
- **页面目标**: 提供同学留言互动功能
- **功能点**:
  - **留言列表**: 展示留言内容，含昵称、时间、留言内容，新留言在顶部
  - **留言提交表单**: 昵称输入框 + 留言内容输入框 + 提交按钮，前端校验非空
  - **本地持久化**: 提交的留言保存到 localStorage，刷新页面不丢失
  - **提交反馈**: 提交成功后显示 toast 提示，表单清空，新留言动画插入列表顶部

### 全局交互
- **顶部导航栏**: 固定在顶部，滚动时背景从透明渐变为实色 + 毛玻璃效果
- **平滑滚动**: 锚点跳转使用平滑滚动动画
- **回到顶部按钮**: 滚动超过一屏后显示，点击平滑回到顶部
- **响应式适配**: 适配手机、平板、电脑三种屏幕尺寸，移动端导航转为汉堡菜单
- **卡片悬停动效**: 所有卡片 hover 时有上浮 + 阴影加深的微动画

---

## 数据共享配置

本项目为单页应用，所有数据通过各模块内部 state 管理，无需跨页面全局共享。留言板数据通过 localStorage 独立持久化。

| 存储键名 | 数据说明 | 使用页面 |
|---------|---------|---------|
| `__class2_guestbook_messages` | 留言板留言列表，类型为 `IGuestbookMessage[]` | 留言板区块 |

```ts
interface IGuestbookMessage {
  id: string;
  nickname: string;
  content: string;
  timestamp: number;
  source: 'mock' | 'user';
}

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 无参考材料，按产品语义与设计要求自主建立视觉系统
- **核心情绪 / 应用类型**: 青春活力、温暖明亮的高中班级展示型单页网站，记录成长、凝聚集体
- **独特记忆点**: 以"2027届毕业倒计时"为叙事锚点，蓝青渐变底色叠加暖橙日光点缀，像教学楼走廊里洒满夕阳的教室窗户

## 2. Art Direction

- **方向名**: 青春日光
- **Design Style**: Soft Blocks 柔色块 + 轻玻璃拟态 —— 柔色块承载班级成员与相册卡片，轻玻璃用于固定导航与悬浮元素，既青春活泼又不失清爽秩序
- **DNA 参数**: 圆角 soft（`rounded-xl` / `rounded-2xl`）/ 阴影 layered（`shadow-md` 叠 `shadow-sm` 柔边）/ 间距 spacious（`gap-6` / `p-8`）/ 字体方向 display 圆润有设计感、body 清晰易读 / 装饰手法：渐变光晕、几何圆形点缀、卡片微抬升
- **应用类型**: Landing —— 单页叙事流，区块按故事节奏纵向排列

## 3. Color System

**色彩关系**: 青蓝主色 + 同色系极浅青灰底 + 暖橙阳光点缀色 + 近纯白卡片承载面
**配色设计理由**: 青蓝传递青春、智慧与清澈，呼应高中校园的天空与校服意象；暖橙用于倒计时、标签、重点数字，像阳光洒在书页上，打破冷色的单一；中性层用近白与浅灰，保证内容长读舒适。
**主色推导**: 主色取自高中校园常见的"晴空蓝→湖水青"过渡，色相在 200°-190° 之间，饱和度中等偏高、明度适中，既有活力又不刺眼；点缀色取 35° 暖橙，与主色形成蓝橙互补，权重控制在 10% 以内。
**使用比例**: 65% 中性（bg + card + border）/ 25% 辅助（青蓝衍生深浅 + accent）/ 10% 暖橙点缀；primary 只用于 CTA、主标题强调、关键数字；accent 与 border 承担次级交互与分隔。

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(195 30% 97%) | 页面背景，极浅青灰，像清晨教室外的天色 |
| card | `--card` | `bg-card` | hsl(0 0% 100%) | 卡片与内容承载面，纯白干净 |
| text | `--foreground` | `text-foreground` | hsl(210 25% 15%) | 标题正文，深墨蓝灰，对比充足 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(210 12% 45%) | 辅助文字、日期、说明 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(198 85% 45%) | 主交互、主标题强调、倒计时数字，青春青蓝 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(0 0% 100%) | 主色上的文字与图标 |
| accent | `--accent` | `bg-accent` | hsl(195 60% 94%) | hover/focus 浅底、选中底、标签背景 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(200 30% 25%) | accent 上的文字与图标 |
| border | `--border` | `border-border` | hsl(200 15% 88%) | 卡片、输入、分隔线，极浅青灰 |

**语义色提示**:
- 成功（文明班级/荣誉）: `hsl(150 55% 45%)`，三态 `bg-emerald-50 / border-emerald-200 / text-emerald-700`，饱和度与 primary 对齐
- 警告（重要公告）: `hsl(38 90% 55%)`，三态 `bg-amber-50 / border-amber-200 / text-amber-800`，与点缀橙色温一致
- 错误: 无需强错误态，如表单校验用 `hsl(0 75% 55%)`，三态 `bg-red-50 / border-red-200 / text-red-700`

## 4. 字体与节奏

- **font-display**: ZCOOL QingKe HuangYou —— 圆润有力的中文标题字体，青春不幼稚，用于 Hero 班级名、板块大标题、倒计时数字
- **font-body**: Noto Sans SC —— 清晰现代的中文无衬线，正文与卡片信息保证长读舒适度
- **字号**: H1 text-5xl ~ text-6xl md:text-7xl；H2 text-2xl ~ text-3xl md:text-4xl；body text-base；muted text-sm
- **圆角**: 大（`rounded-xl` / `rounded-2xl`）—— 呼应青春柔软气质，卡片与按钮均使用大圆角

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导
- **Page / Section Order**: Hero → 班级介绍 → 班级成员 → 班级动态 → 班级相册 → 课程表 → 荣誉墙 → 留言板 → 页脚，与需求 9 大模块 1:1 对齐
- **Standard Content Zone**: `max-w-6xl mx-auto`，适合展示型 Landing 的阅读行长与卡片栅格
- **Shell / Frame Alignment**: 固定顶栏与内容区同宽对齐，导航 chrome 滚动时由透明渐变至毛玻璃
- **Padding & Rhythm**: `px-4 md:px-6 lg:px-8 py-16 md:py-24`，区块垂直间距 `gap-12 md:gap-16`，保持 8px 倍数
- **Full-bleed Zones**: Hero 背景、荣誉墙时间线装饰、页脚背景可 `w-full`，内部文字与卡片仍受 Standard Content Zone 约束
- **Local Narrowing**: 班级简介正文、留言表单可局部收窄至 `max-w-3xl`
- **Overflow Strategy**: 课程表、相册分类横滑标签使用 `overflow-x-auto`
- **Flexibility Boundary**: 允许移动端卡片列数、内边距与字号微调；全局 max-w、圆角、主色、阴影语言保持一致

## 6. 视觉与动效

- **装饰**: 渐变光晕圆斑 + 几何细线分隔 + 轻玻璃导航
- **阴影/边界**: 轻至中，卡片用 `shadow-md` + 1px 内光感边界，悬停时 `shadow-lg` 微抬 2px
- **动效**: 精致克制 —— 卡片 hover 微抬升 + 阴影加深 + 边框变亮；页面滚动时区块渐入上移；导航栏透明→毛玻璃过渡；灯箱打开缩放淡入；所有动效 200-300ms ease-out

## 7. 组件原则

- 按钮、输入框、卡片、标签页必须有 Default / Hover / Active / Focus / Disabled 状态
- Primary 按钮用于留言提交、回到顶部等主行动；Outline/Secondary 用于导航与次级操作；菜单项与标签用 accent 承接 hover/selected
- 课程科目标签使用不同低饱和彩色背景 + 深色文字，饱和度统一对齐 primary
- 空状态与加载骨架延续青蓝 + 暖橙的视觉语言，不回退默认样式
- `focus-visible` 使用 2px primary 外轮廓 + 4px accent 扩散环

## 8. Image Direction

- **Image Role**: Hero 背景图 + 相册内容图 + 班级成员头像占位
- **Image Art Direction**: Hero 图为校园场景氛围图，清晨或傍晚的教学楼走廊/操场/教室窗边，阳光斜射、空气中有尘光，画面偏暖青色调，留有大面积负空间用于叠放班级名与口号；相册图为真实校园活动场景（运动会、班会、文艺汇演、自习、团建），构图自然生活化，避免摆拍感；成员头像使用统一风格的插画或带轻微滤镜的真实人像占位
- **Image Prompt Keywords**: high school campus, morning sunlight through classroom windows, soft blue sky, warm orange glow, dusty light beams, corridor perspective, youth atmosphere, film grain texture, soft focus background, vibrant yet warm color palette
- **Image Avoidance**: 避免商务风校服假人素材、避免过度饱和的卡通校园插画、避免无主体的纯渐变抽象图、避免低像素网图水印感、避免摆拍僵硬的集体照

## 9. Anti-patterns

- **Split personality**: 板块之间各自为政，有的圆角大有的方、有的蓝有的紫；全站统一青蓝主色 + 暖橙点缀 + 大圆角 + 柔阴影
- **Phantom tokens**: 编造不存在的 CSS 变量；只使用定义的 9 个基础 token + 语义色三态
- **Default SaaS drift**: 回到通用企业蓝 + 白卡 + 无意义数字展示；用倒计时叙事、校园光感、班级成员人格化卡片建立独特记忆
- **Invisible interaction**: 只做 hover 不做 focus-visible；每个可交互元素都有键盘可达的清晰外轮廓
- **Mono-hue tyranny**: 按钮、标签、图标、边框、链接全用 primary 青蓝；按 65-25-10 比例分配，暖橙只给重点数字与关键点缀
- **Status color drift**: 成功/警告色饱和度过高抢主色风头；语义色饱和度与 primary 对齐 ±15%
- **Album chaos**: 相册图片尺寸不一、风格混杂；统一使用 4:3 比例裁剪，灯箱查看原图